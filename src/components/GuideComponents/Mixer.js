import React from "react";
import "../../Style/mixer.css";

const Mixer = () => {
  return (
    <div className="mixerdiv">
      <img src="https://i.imgur.com/BU1xceM.png" className="mixer" alt="..." />
      <h4 id="mixer">
        We use Yamaha CL5 mixer in our main sanctuary. CL5 has 72 channels and
        has many different sound effects and features. In this guide, we will go
        over basic features that you can learn to get familiar with this
        console. In order to turn this mixer on, on your right bottom side,
        there is a key that is already inserted. You can enable the start button
        by turning that key twice. Then, click the on button
      </h4>
    </div>
  );
};

export default Mixer;
